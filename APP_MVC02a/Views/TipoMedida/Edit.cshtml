@model APP_MVC02a.Models.tipo_medida

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>tipo_medida</legend>

        @Html.HiddenFor(model => model.idtipo_medida)

        <div class="editor-label">
            @Html.LabelFor(model => model.descrip)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.descrip)
            @Html.ValidationMessageFor(model => model.descrip)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Regresar al Listado", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
